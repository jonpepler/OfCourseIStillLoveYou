Settings
{
  EndPoint = localhost
  Port = 5077
  Width = 768
  Height = 768
  // Auto-enable streaming on every Hullcam when the active vessel changes.
  // Defaults to false to preserve the manual click-to-enable workflow for
  // vanilla installers. Flip to true if you control the cameras programmatically
  // (e.g. via gonogo).
  AutoStream = false
}
